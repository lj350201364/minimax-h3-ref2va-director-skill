---
name: minimaxh3-vibe
description: 将图片、视频、音频参考与简短创意扩写为 MiniMax H3 Ref2VA 英文六段提示词，强调标签映射、口型物理合理性、道具账本和逐镜状态继承。适用于全参考视频提示词扩写、单人氛围短片及连续性修订，不负责直接渲染视频。
---

# minimaxh3氛围感

把用户参考素材和创意转为结构化、物理合理、强连续性的长提示词。交流用中文，提示词正文用英文；对话、歌词和场景可见文字保留原语言。交付完整提示词，不只给修改片段。

## 输入与流程

1. 识别 reference generation、keyframe completion、video editing、video continuation、audio reuse 或 audio reference，可组合。遵循用户的主题、人数、风格、时长与声音要求。
2. 按各类素材上传顺序编号，说明 Picture / Video / Audio 与主体的映射。只有实际提供且可读取的素材才能作为已观察依据；关键素材缺失或无法读取时，向用户索取或说明限制，不编造参考内容。
3. 细读可见发型、发饰、妆容、服装层次、配饰、环境、文字；视频还需观察动作和结束状态，音频需分清复制、音色参考或节奏参考。
4. 分配标签，锁定外观、位置和道具账本，再安排动作、情绪弧、镜头与声音。单人氛围短片读取 [references/vibe-direction.md](references/vibe-direction.md)；其他题材不强套该模板。
5. 按下列六段编写并自检，最后按回复格式交付。

用户未指定时默认 10 秒、4 镜，切点为 00:02.500 / 00:05.000 / 00:07.500。用户指定时长时同步修改 summary、切点、动作量、声音节奏和结束时间。原提示词采用 4–15 秒范围，视为本工作流的目标范围，不宣称为平台实时能力；超范围时说明并询问是否拆段，不擅自截短。第一镜从零开始但不写时间戳；后续切点严格递增且小于总时长，最后一镜明确保持至总时长。镜头数和动作量服从可执行性。

## 固定六段

代码块内仅使用以下字段，顺序和拼写不可变：

1. `subject_definitions`
2. `summary`
3. `retention_analysis`
4. `detailed_description`
5. `overall_soundscape`
6. `non_diegetic_music`

### subject_definitions

- `<Subject N>` 定义可复用可见内容：人、环境、服装、风格，可由多个资产共同定义。同一人物可合并多张参考，不同人物分别绑定身份，避免混脸、服装串换和肢体混淆。
- 图片只作为角色或场景来源时，写入对应 Subject 定义；只有作为首帧、关键帧或构图锚点时，才让 `<Picture N>` 独立成行。
- `<Video N>` 表示整视频关系，如剪辑、续写、运镜节奏参考。
- `<Audio N>` 表示复制、音色或节奏参考。对应说话人的音频明确关联 `<Subject N> (Sx)`。
- 编号对应真实上传顺序；一旦建立，全六段含义一致。

### summary

以 `[reference generation]` 等任务类型前缀开头，可组合。用一段英文概括总时长、目标内容、镜头结构和参考关系。

### retention_analysis

每个独立定义的标签一行，注明出现或应用镜头和保留内容。仅嵌入 Subject 定义作为来源的 Picture 不必重复成行。

- 可见类标记：`fully_preserved` / `partially_preserved` / `attribute_transfer` / `weak_reference`。
- 音频类标记：`fully_copy` / `partially_copy` / `reference` / `weak_reference`。
- 标记与实际改动一致；例如重新安排场景布局时不能宣称布局完全保留。

### detailed_description

首段写整体摄影风格、光线、景深和色调，再用 `Global rules:` 写身份、口型、道具与空间约束。

- 第一镜标作 `[Shot 1]`，不写时间戳；之后使用 `[Shot N] At MM:SS.mmm`。
- 每镜写清构图、主体外观和位置、环境光线、动作与状态变化、运镜、声音。
- 每镜结尾必须写 `End state: ...`，记录姿态、目光、双手、道具数量与位置。下一镜开头明确继承上一镜结束状态，随后写可见过渡动作，不瞬移、不重置姿势。
- 发言用 `(S1)` / `(S2)`，对话用 `<d>[中文] 原文</d>` 等标签。除用户要求外，不给单人 vibe 添加台词。

### overall_soundscape

写环境底噪和物理音效；特殊音效点名对应镜头，与可见接触动作同步，不在这里混入非剧情配乐。

### non_diegetic_music

写风格、乐器、情绪曲线，以及在哪镜 swell、在哪镜 fade。用户要求无配乐时写 `No non-diegetic music.`，不写 N/A。音频全复制时不额外添加冲突音乐，也不一边声明 fully_copy 一边修改音频。

## 口型物理约束

- 禁止边咬、边嚼边说清晰台词。咀嚼时嘴唇闭合，只允许描述为发闷的声音，例如 `<d>[中文] 嗯~！</d>`。
- 清晰台词顺序必须为：嚼完，出现喉咙微动的可见吞咽，空嘴开口，并写 `crisply articulated with an empty mouth`。
- 说话者若手持食物或道具，先写清放置动作和位置，再开口。
- 把适用口型限制写入 Global rules。无进食场景不凭空添加食物、吞咽表演或台词。

## 道具与场景连续性

- 开场账本锁定所有可数、可互动道具的数量和位置，写明不得出现清单外道具。石子、落叶等一旦被拿、踢或接，也须预先入账；背景不可数纹理不必逐粒列举。
- 拿、递、放按 `(a)` 接近与接触、`(b)` 转移、`(c)` 到位和松手逐步描写，实时更新手中与原位置的数量。物体被拿起后不得同时留在原处；被吃掉的部分明确从账本扣除。
- 穿戴道具固定在参考图的原始佩戴位置，不消失、不移到其他位置、不落入手中。“不上脸”指不得从头顶等位置滑到脸上；本来戴在脸上的眼镜保持原位。小动作不得移动这些固定配饰。
- 远处人、船、车默认 `tiny, blurred, nearly still`。用户明确要求远景活动时，按要求写受控运动和位置变化。
- 场景文字保留原拼写且清晰可读；无法辨认的文字不猜写，可向用户索取准确内容。
- 椅子、栏杆和建筑位置锁定。环境动态可用风、发丝、布料或树叶响应表现，但不能改变静态布局。

## 回复与自检

先用中文简述上传映射、设计映射（公式、动作、场景母题）和连续性要点；再在一个代码块中输出完整英文六段；最后给简短说明和可选迭代方向。用户明确要求只给提示词时省略前后说明。

输出前检查：标签及身份一致；字段齐全；总时长和切点正确；每镜 End state 与下一镜开头接续；道具数量守恒；对话空嘴并先放下道具；配饰与远景锁定；原文字保留；动作和台词能在时长内完成；声音与动作一致。过密时减少动作，不牺牲连续性。不得把提示词交付描述为已渲染视频或已通过 H3 实测。
